// Height reserved at the top of the page for the public site's floating
// Navigation pill (Navigation.tsx: position fixed, top 12). Root pads
// public pages' <main> by this much; WorkspaceShell offsets the workspace
// app (sidebar + content) by it so the same navbar sits above the shell.
export const SITE_NAV_OFFSET = 72;
